## 2026-09-25

- Phase D1 core loop: 6x6 spatial-knapsack-into-combat mechanic (grid/items/energy-routing/Back-EMF/Panic Cut), headless ASan+UBSan test suite (53 checks), and a local SDL2 debug shell with a headless --selftest. Backpack battler VS1 mechanic from DEADWEIGHT/NORTHSTAR.md, spun into its own repo. (sess-20260923-1030-4a526255)
- Phase D2: dw2_server, a real server-authoritative TCP match server (poll() loop, embedded FIFO queue -- no separate matchmaker binary, a real correction to NORTHSTAR.md's original plan). New core/protocol.{h,c} wire codec for a packing-phase-then-real-time-tick-combat lifecycle; ported core/net.h/http.{h,c}/iduna.{h,c} infra from DEADWEIGHT. IDUNA: internal/games.Registry["deadweight_2"], a new DEADWEIGHT2-SERVER M2M agent, migration 202609250900_deadweight2_agents_and_permissions.sql. New tools/dw2_test_client.c + a scripts/build.sh smoke test: a real loadout beats an empty grid in a real match over the actual wire protocol, verified alongside placement/cut rejection, pack-deadline auto-start, and forfeit-on-disconnect. New .github/workflows/ci.yml. (sess-20260923-1030-4a526255)
- D2 (folded, was scoped as a separate D3 -- founder real-time "just call it D2"): `apps/client/main.c` (`dw2_client`), a real interactive SDL2 client speaking `core/protocol.h`'s actual wire protocol -- connect/auto-queue/pack (cursor-based, same controls as `apps/local`)/real-time combat (server-tick-driven, live Panic Cut by placement index) against a real `dw2_server`. Optional IDUNA guest auth (`--iduna-url`, `--guest-file` persists identity) or `--token`. `--selftest`/`--empty-grid` drive the same real code paths headlessly; wired into `scripts/build.sh` as a second smoke match alongside the existing `tools/dw2_test_client.c` protocol-edge-case test (kept, unchanged). Two real bugs found and fixed live during this pass: the connect handshake sent AUTH unconditionally whenever a token was in hand, breaking `--no-auth` servers (fixed with a short WELCOME-or-silence probe); an inverted return-value check then made the fix itself silently skip sending AUTH against a genuinely auth-required server. (sess-20260923-1030-4a526255)
- Principle 21: `scripts/generate_construct.sh` (git-ls-files based, ported from DEADWEIGHT's own, locally verified deterministic) + CI wired to generate it, verify byte-for-byte determinism, and bundle a copy into each real client's own zip (`D2_Client_<build>.zip`, `D2_Local_<build>.zip`) before upload -- same pattern as SHANKPIT's own release.yml. Also ships standalone as both `.zip` and `.gz` (an explicit, deliberate exception to this monorepo's LZ4-by-default convention). Every artifact filename carries a `build_<run number>_<short sha>` tag so consecutive CI runs never collide under a generic name. (sess-20260923-1030-4a526255)
- New `core/palette.h` (DEADWEIGHT's real brand hex palette, `docs/BRAND_STYLE_GUIDE.md`'s brutalist terminal identity), `core/font.{h,c}` (D2's first real text renderer -- the same 5x7 bitmap font DEADWEIGHT's desktop client ships), `core/sfx.{h,c}` (DEADWEIGHT's actual real sound effects -- a procedural synth engine ported near-verbatim, since DEADWEIGHT has zero sample audio files anywhere; D2-specific cues newly composed for Railgun fire/Panic Cut/Back-EMF shatter/match results in the same synthesis idiom). Live-verified: an offline-render smoke test confirms all 15 cues produce real, non-silent, non-clipping audio. Integration into `apps/local/main.c`/`apps/client/main.c` held pending a concurrent in-progress combat-redesign pass on those files. (sess-20260923-1030-4a526255)
- D2 combat redesign (`EMILY/BACKLOG.md` SECTION 548, founder real-time: "a mini game in between ship auto battler rounds" + "swingy...comeback mechanics and bluff strategies"): combat now runs in fixed `DW2_ROUND_TICKS`-tick rounds; each round-break opens a real-time "Surge Timing" skill check -- a server-authoritative, wall-clock-measured reaction window (`DW2_S_ROUND_BREAK` -> `DW2_C_ROUND_CALL` -> `DW2_S_ROUND_RESULT`, new additive `core/protocol.h` messages) graded PERFECT/GOOD/MISS/NONE against a seeded, both-sides-identical target. Each call also carries a hidden Overcharge/Brace pick (`core/round.h`/`round.c`), revealed only after both sides commit -- a real bluff layer with an asymmetric payoff table (Overcharge: high ceiling, backfires with self-damage on a missed timing check; Brace: safe armor bonus, whiffs for free) -- amplified further when the calling ship is behind on hull% (the comeback lever), wired into `dw2_ship_tick` via a new `Dw2Ship.dmg_mult` field, the only change to existing D1 combat rules. `apps/server/main.c` grades every break server-side (no client-timestamp trust); `apps/client/main.c` binds O/B during combat; `tools/dw2_test_client.c` gained `--round-call`. Live-verified over the real wire protocol: a new `scripts/build.sh` smoke test shows one well-timed Overcharge call turning an otherwise-exact tie (two identical loadouts) into a win three ticks early; 43 new unit checks (96 total) pin the grading windows and payoff table exactly, including the behind-amplification numbers. Full design writeup in `docs/COMBAT_REDESIGN.md`. (sess-20260925-claude-95f8453d)
- D2 "cannon programming" (`EMILY/BACKLOG.md` SECTION 549, founder real-time: "add in LO programming to actual gameplay... like cannon programming or something"): `dw2_ship_tick`'s weapon-fire check is now a real, compiled LO program (`cannon/cannon_decision.llll` -> `.prn` -> PARENA-generated C, `core/cannon.{h,c}`), not a hardcoded rule -- packs the same `behind` signal SECTION 548 already computes plus whether this shot would be lethal into one real, naturally-4-valued state, matching LO's own real mod-4 arithmetic ceiling (checked directly against `LO/NORTHSTAR.md`'s own DUNG-integration precedent, not assumed). Shipped decision is FIRE in all 4 states -- byte-identical to the rule it replaces, verified: every existing hand-derived smoke-test tick count is unchanged. A second, real, standalone-tested example program (`cannon_bank_on_safe_lead.llll`) proves the mechanism can express real conditional strategy (holds fire on a safe, non-lethal lead) without being wired live, since holding has no compensating mechanical payoff in this V0 -- a real, honestly-discovered failure mode (it could stall a match against an unarmed opponent forever), not glossed over. `core/parena_runtime.{h,c}` vendored unmodified from PARENA (public domain); built with `PARENA_NO_GRAPHICS` (that header's own documented escape hatch) so `dw2_server`/`test_core_loop` stay genuinely SDL-free. `cannon/*.prn`/`*_gen.c` are committed, generated artifacts (regenerated via new `scripts/generate_cannon.sh` only when a `.llll` source changes) -- ordinary `scripts/build.sh`/CI never needs a sibling LO/PARENA checkout. New `tests/test_cannon_hold.c` (a standalone binary proving the HOLD branch live, through a real `dw2_ship_tick` call) and `tools/dw2_cannon_demo.c`; 4 new unit checks in `tests/test_core_loop.c` (100 total). Real, honest finding: LO's own already-named stdlib gaps (matrices, PCRE matching) turned out unrelated to this feature -- zero new LO stdlib work was needed. Full design writeup in `docs/LO_CANNON_PROGRAMMING.md`. (sess-20260925-claude-95f8453d)

